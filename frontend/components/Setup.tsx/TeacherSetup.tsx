import React from 'react'

import { ISetupProps, ISetupStep } from '../../types/setup'

const teacherSetupSteps: ISetupStep[] = [
    
]

const TeacherSetup = (props: ISetupProps) => {
    const { key } = props
    switch (key) {

    }
}
