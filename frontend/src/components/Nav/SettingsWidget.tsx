import React from 'react'

import NavItem from './NavItem'

const SettingsWidget = () => {
    return (
        <div>
            <NavItem
                onClick={() => null}
                title='Settings'
                icon='settings'
            />
        </div>
    )
}

export default SettingsWidget
